# Workflows

| Workflow | File | Purpose |
| --- | --- | --- |
| Node CI | `.github/workflows/ci.yml` | `npm ci` + lint + test + build on `develop` / `master` PRs |

Scorecard / Codecov / Sonar may be added when the package leaves scaffold.
