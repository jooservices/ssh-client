# Used by future live smoke only — never commit real values.
VIGOR_HOST=192.168.1.1
VIGOR_PORT=22
VIGOR_USER=admin
VIGOR_PASSWORD=
# Required for live connect (OpenSSH SHA256:… or hex):
# VIGOR_SSH_HOST_FINGERPRINT=SHA256:xxxxxxxx
# Tests / simulated hosts only:
# VIGOR_SSH_INSECURE_SKIP_VERIFY=true
